# Angular static builds read the runtime value from public/app-config.js.
# Replace API_BASE_URL there during deployment, or inject the same value into
# the generated static asset as part of the hosting build.
API_BASE_URL=http://localhost:8080/api
