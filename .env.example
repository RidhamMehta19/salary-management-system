# Local defaults are documented here for convenience. Copy values into your
# shell or platform configuration; never commit real credentials.
DATABASE_URL=jdbc:postgresql://localhost:5432/salary_management
DATABASE_USERNAME=salary_user
DATABASE_PASSWORD=salary_password
CORS_ALLOWED_ORIGINS=http://localhost:4200
API_BASE_URL=http://localhost:8080/api
# Optional only if host tools need direct database access:
# POSTGRES_HOST_PORT=5433
